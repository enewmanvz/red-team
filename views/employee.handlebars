{{> employeemenu}}
<div class="container">
    <h3>Welcome to Employee Dashboard</h3>

</div>
<div class="container">
    <h4>You are currently assigned to the following specific site</h4>
    <div class="card" style="width: 18rem;">
        <img src="..." class="card-img-top" alt="...">
        <div class="card-body">
            <h5 class="card-title">Site Name: {{singleWarehouse.name}}</h5>
            <p class="card-text">Location: {{singleWarehouse.location}}</p>
             <p class="card-text">Capacity: {{singleWarehouse.capacity}}</p>
        </div>
    </div>

</div>